import { PIQ_REQUIREMENTS } from "@/config/piq";
import { GDCRequirement } from "@/types";

export function getAllRequirements(): GDCRequirement[] {
  // For now return the PIQ_REQUIREMENTS as the GDC requirement set (stub)
  return (PIQ_REQUIREMENTS as unknown) as GDCRequirement[];
}
